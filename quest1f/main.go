package main

import (
	"fmt"
)
 // print comb2 a program that print digt in accending order

 func main(){
	for i := 0 ; i <= 8; i ++{
		for j := 0+1 ; <= 9; j++{
			fmt.Printf(" %d %d,", i, j)
			if i < j (
				continue
			)
		}
	}

	fmt.Println()
 }